{
  "name": "site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "bash ../scripts/build-wasm-web.sh",
    "build": "bun run src/build.ts && bun build ./src/index.ts ./src/scan.ts --outdir dist --target browser --minify",
    "dev": "bun run src/build.ts && bun build ./src/index.ts ./src/scan.ts --outdir dist --target browser --watch",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check",
    "format": "biome format --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.1"
  }
}
