{
  "name": "site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run src/build.ts && bun build ./src/index.ts --outdir dist --target browser --minify",
    "dev": "bun run src/build.ts && bun build ./src/index.ts --outdir dist --target browser --watch",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  }
}
